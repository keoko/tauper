<h1><%= gettext("New Game") %></h1>

<%= render "form.html", Map.put(assigns, :action, Routes.game_path(@conn, :create)) %>

<span><%= link gettext("Back"), to: Routes.page_path(@conn, :index) %></span>
